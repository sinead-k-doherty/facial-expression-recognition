version: "3"

services:
    fer_model:
        build: .
        volumes:
            - ./app/:/fer_model/app/
            - ./training_images/:/fer_model/training_images/
        ports:
            - 5000:5000
        command: bash -c "python ./app/main.py"
